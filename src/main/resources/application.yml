spring:
  mvc:
    path match:
      matching-strategy: ant_path_matcher

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${database.url}
    username: ${database.username}
    password: ${database.password}

database:
  url: jdbc:mysql://localhost:3306/restaurant?useUnicode=ture&characterEncoding=utf-8&useSSL=true&requireSSL=false&verifyServerCertificate=false
  username: root
  password: 123456

